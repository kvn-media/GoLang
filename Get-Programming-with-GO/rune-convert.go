package main

import (
	"fmt"
)

func main() {
	var (
		pi    rune = 960
		alpha rune = 940
		omega rune = 969
		bang  byte = 33
	)
	fmt.Print(string(pi), string(alpha), string(omega), string(bang)) // Prints πάω!
}